export type AvatarVariant = {
  name: 'round' | 'square';
  border: string;
  padding: string;
  rounded: string;
  shadow: string;
  borderColor: string;
};

export type Variant = 'rounded' | 'square';
