export type ButtonVariants = {
  name: string;
  background: string;
  hover: string;
  border: string;
};

export type ColorVariants = {
  name: string;
  colors: string;
};

export type Variant = "blue" | "purple" | "transparent" | "disable";
export type Color = "white" | "blue";
